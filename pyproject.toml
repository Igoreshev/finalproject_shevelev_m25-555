[tool.poetry]
name = "finalproject-shevelev-m25-555"
version = "0.1.0"
description = ""
authors = ["Shevelev"]
readme = "README.md"

packages = [{ include = "final_project" }]

[tool.poetry.dependencies]
python = ">=3.12"

[tool.poetry.scripts]
project = "final_project.main:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
